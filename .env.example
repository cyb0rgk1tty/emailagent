# =============================================================================
# REQUIRED CONFIGURATION
# These environment variables MUST be set for the application to start
# =============================================================================

# Database Configuration
DB_USER=emailagent_user
DB_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD_32_CHARS_MIN
DB_HOST=postgres
DB_PORT=5432
DB_NAME=supplement_leads_db

# Security - REQUIRED (must be at least 32 characters)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARS

# AI Configuration - REQUIRED for PydanticAI agents
# Sign up at: https://openrouter.ai/
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-YOUR_API_KEY_HERE

# =============================================================================
# EMAIL CONFIGURATION
# Required for email monitoring functionality
# =============================================================================

EMAIL_IMAP_HOST=imap.yourdomain.com
EMAIL_IMAP_PORT=993
EMAIL_SMTP_HOST=smtp.yourdomain.com
EMAIL_SMTP_PORT=587
EMAIL_ADDRESS=contact@yourdomain.com
EMAIL_PASSWORD=YOUR_EMAIL_APP_PASSWORD

# Optional: CC recipients for all outgoing emails (comma-separated)
EMAIL_CC_RECIPIENTS=

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

ENVIRONMENT=development
DEBUG=false

# Redis
REDIS_URL=redis://redis:6379/0

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================

# Recommended setup:
# - Extraction: Claude Haiku (fast, cheap for data extraction)
# - Response: Claude Sonnet (high-quality for email drafting)
OPENROUTER_MODEL=anthropic/claude-sonnet-4.5
OPENROUTER_EXTRACTION_MODEL=anthropic/claude-haiku-4.5
OPENROUTER_RESPONSE_MODEL=anthropic/claude-sonnet-4.5

# Model Settings
LLM_TEMPERATURE_EXTRACTION=0.3
LLM_TEMPERATURE_RESPONSE=0.7
LLM_MAX_TOKENS=4000
LLM_TIMEOUT=60

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Historical Email Backfill (for AI learning from past responses)
# Leave commented out if not using this feature
#HISTORICAL_EMAIL_ADDRESS=your.email@domain.com
#HISTORICAL_EMAIL_PASSWORD=YOUR_PASSWORD
#HISTORICAL_IMAP_HOST=imap.domain.com
#HISTORICAL_IMAP_PORT=993
#BACKFILL_SUBJECT_FILTER=Contact Form:
#BACKFILL_LOOKBACK_DAYS=365
#BACKFILL_MAX_EMAILS=1000

# OpenAI API key for alternative embeddings (not required - uses OpenRouter)
OPENAI_API_KEY=

# Error Tracking (Optional)
SENTRY_DSN=

# CORS Origins (JSON array format for additional origins)
# CORS_ORIGINS=["http://localhost:3000","https://yourdomain.com"]

# =============================================================================
# PGADMIN (Only if using the tools profile)
# Required only when running: docker compose --profile tools up
# =============================================================================
#PGADMIN_EMAIL=admin@yourdomain.com
#PGADMIN_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
